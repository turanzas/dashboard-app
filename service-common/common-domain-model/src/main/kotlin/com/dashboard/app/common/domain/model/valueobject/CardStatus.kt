package com.dashboard.app.common.domain.model.valueobject

/**
 * Enum class representing the status of a card.
 *
 * @property ACTIVE Indicates that the card is active and can be used.
 * @property INACTIVE Indicates that the card is inactive and cannot be used.
 */
enum class CardStatus {
    ACTIVE,
    INACTIVE,
}