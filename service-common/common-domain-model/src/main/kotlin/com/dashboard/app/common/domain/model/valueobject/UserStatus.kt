package com.dashboard.app.common.domain.model.valueobject

/**
 * Represents the status of a user in the system.
 */
enum class UserStatus {
    ACTIVE,   // The user is active and can perform all operations.
    INACTIVE, // The user is inactive and cannot perform any operations.
}