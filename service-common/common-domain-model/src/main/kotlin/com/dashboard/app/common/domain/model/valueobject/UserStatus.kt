package com.dashboard.app.common.domain.model.valueobject

/**
 * Enum class representing the status of a user.
 *
 * @property ACTIVE Indicates that the user is active and can perform all operations.
 * @property INACTIVE Indicates that the user is inactive and cannot perform any operations.
 */
enum class UserStatus {
    ACTIVE,
    INACTIVE,
}